# 相似

>[!NOTE] **定义**(相似变换)
>矩阵 $A,B$ 相似：如果存在可逆矩阵 $P$，使得 $B = P^{-1}AP$。

相似的本质是线性变换在不同基下对应的表示矩阵，故有如下不变量：
> **相似不变量**：$\det A$，$\mathrm{tr}(A)$，$\mathrm{rank}(A)$，$\det(\lambda I - A)$，极小多项式(零化多项式)。

所有矩阵都可以相似于一类特殊的矩阵，一般我们称它某类特殊的矩阵为标准型。[Jordan标准型](mathhub/Jordan标准型.md)